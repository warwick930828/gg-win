var onChangeCheckhk6 = onChangeCheckhk6 || {};
var onChangeCheckhk6 = {
    check_11: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_21: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_31: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_32: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_33: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_34: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_35: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_36: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_41: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_51: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_52: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_53: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
    check_54: function(betData) {
        var betCount = 1;
        $.each(betData, function(key, value) {
            betCount *= value.length;
        });
        return betCount;
    },
}
